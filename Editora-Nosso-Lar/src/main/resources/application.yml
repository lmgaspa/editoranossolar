spring:
  datasource:
    url: jdbc:mysql://localhost:3306/editora_db
    username: root
    password: ${ADMIN_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8080
